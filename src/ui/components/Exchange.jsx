import { Box, Typography } from "@mui/material";

export default function Exchange() {
    return (
        <Box>
            <Typography>This is the exchange component</Typography>
        </Box>
    );
}